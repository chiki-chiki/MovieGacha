
# 映画ガチャアプリ 検討ログ

このファイルはClaudeとの壁打ち内容を時系列で要約したもの。実装は自分で行う前提で、方針・判断の記録として残す。
正式な仕様は [movie-gacha-spec.md](movie-gacha-spec.md) 側にまとめてあり、こちらは議論の経緯・判断理由のメモ。

---

## 2026-08-18: 初期仕様の壁打ち

- コンセプト: 映画を布教したい人がリストを作成 → 見る映画に迷った人がリストを選んでガチャを引くとランダムに1本選ばれる
- 詰めた内容(詳細は [movie-gacha-spec.md](movie-gacha-spec.md) 参照)
  - 想定ユーザー: 友人・少人数グループ(※後日 8/21 に方針転換、下記参照)
  - 映画データ: TMDB APIで検索・自動取得
  - ユーザー識別: ログインなし、表示名入力のみ(保存なし)
  - リスト公開範囲: 全公開
  - ガチャ: 完全ランダム、重複除外や重み付けなし
  - データ永続化: サーバー側DBが必要
  - MVP画面: リスト作成/TMDB検索登録、リスト一覧・選択、ガチャ演出、リスト編集・削除
  - デバイス: PC・スマホ両対応のレスポンシブ
  - スコープ外: 本格認証・セキュリティ対策、多言語対応、視聴履歴連携
- 仕様書 `movie-gacha-spec.md` を作成
- ここで「勉強のため1から自分で作る」と方針決定。以降Claudeは実装せず壁打ち役に徹する

## 2026-08-19: Web開発プロセスの一般論

- 「仕様固め→技術選定→基本設計→詳細設計→実装→ユニットテスト→結合テスト→システムテスト→ユーザテスト→リリース→メンテナンス→追加開発」という流れは、典型的なウォーターフォール/V字モデルとしては正しい
- 個人学習用途向けの補足
  - 基本設計・詳細設計は分けずに「設計」でまとめてよい
  - ユニットテストは実装と同時並行(TDD寄り)が今の主流
  - 結合・システムテストの境界も厳密でなくてよい
  - ユーザーテストは「友人に触ってもらう」程度で十分
  - 技術選定と仕様固めは実際は行ったり来たりする
- 学習効率を優先するなら「小さい機能単位で 設計→実装→テスト を繰り返す反復開発」を推奨

## 2026-08-19: 技術選定の要素

「フロントエンド/バックエンド/DB/外部API」の4項目に加えて、以下も検討対象に入れるべきと指摘
- ホスティング/デプロイ先(フレームワークとは別の意思決定)
- UIライブラリ/CSSフレームワーク(ガチャ演出のアニメーション実装コストに直結)

バージョン管理(Git一択)やパッケージマネージャは個人開発では深く悩まなくてよい。

## 2026-08-21: 技術選定レビュー

選定内容:
- フロントエンド: React + Next.js
- バックエンド: Hono(TypeScript)
- DB: D1
- ホスティング: Cloudflare
- 外部API: TMDB

レビュー結果:
- **Hono**: 「少し前のトレンド」ではなく、Cloudflare公式が推す現行の主流。良い選択
  - `hono/client`(RPC機能)でフロント⇔バック間の型を自動共有できる → 型安全の目的にも合致、採用を推奨
- **D1**: この規模のデータ量なら性能面は問題なし。SQLiteベースで複雑クエリには不向きだが今回は無関係。Cloudflareへのロックインが強まる点は認識しておく
- **TMDB**: 特に懸念なし
- **最大の懸念点: Next.js × Cloudflare Workers/Pagesの相性**
  - Next.jsは元々Node.js/Vercel向けで、Cloudflareで動かすには`@opennextjs/cloudflare`等のアダプタが必要。Node.js API制限やImage Optimization周りに制約が残る
  - 選択肢1: Next.jsもCloudflareに寄せる(アダプタ使用、単一ベンダーで完結するがハマりどころのリスクあり)
  - 選択肢2: Next.jsはVercel、Hono+D1はCloudflare Workersのまま(Next.js側は無変換でネイティブ動作、CORS設定は必要、ダッシュボードは2つになる)

### Next.jsの代替候補(参考として検討)

- **Vite + React(SPA)**: Cloudflare Pagesにそのまま置ける、アダプタ不要でハマりどころがほぼない。SEO/SSR不要なら有力
- **React Router v7(旧Remix)**: Cloudflare Workersを公式サポート対象にしており、Next.js的なフルスタックReact体験をアダプタなしで得られる
- **Hono自身のJSX機能でSSR**: バックエンドと同一Workerで完結できるが、素のReactの学習にはならないため今回の目的には非推奨

### 方針転換: 対象ユーザーを「友人向け」から「広く公開」へ

- 変更理由: どうせ作るなら多くの人に使ってもらうことを視野に入れたい
- この変更によりSSRの必要性が強まると判断。根拠は一般的なSEO論よりも実務的な理由:
  - **OGP(リンクシェア時のプレビュー)生成にはSSRが前提**。Twitter/LINE/Slack等のbotはJSを実行しないため、リスト/ガチャ結果ページごとにサーバー側でメタタグを動的生成する必要がある。SPAでは基本的に実現不可
  - 検索エンジンからの流入を狙う場合もSSRの方が確実
- 結論: **Next.js on Vercel + Hono/D1 on Cloudflare Workers** の構成が、公開を見据えるなら妥当と判断
  - ダッシュボードが2つになる運用コストは許容
  - フロントとAPIのホスティングを分けるのは実プロダクトでもよくある構成であり、「学習のための妥協」に留まらない
- 今後の検討事項として明記: 不特定多数への公開を視野に入れる場合、当初スコープ外とした**認証・不正利用対策(他人のリスト削除防止、TMDB APIの濫用対策など)** の再検討が将来的に必要になる(今すぐ着手する必要はないが、公開段階で見直すポイントとして記録)

---

## 未決定・today以降の検討事項

- ガチャ演出の具体的な見た目・所要時間
- 表示名の重複や入力バリデーションの扱い
- リストの最小/最大映画数の制約有無
- 公開範囲拡大に伴う認証・不正利用対策の要否とタイミング
