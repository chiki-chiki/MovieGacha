# 映画ガチャアプリ 仕様書 v0.1

## 1. コンセプト

- 映画を布教したいユーザーが「映画リスト」を作成する
- 見る映画に迷ったユーザーはリストを選び、「ガチャを引く」とリストの中からランダムに1本の映画が選ばれる
- 友人・少人数グループ内での利用を想定した、軽量なプロトタイプ

## 1-1. 対象範囲・想定ユーザー

- 想定規模: 友人・少人数グループ(数人〜数十人程度)
- アカウント登録・ログインは行わない。リスト作成時に表示名をその都度入力するのみ(保存・認証なし)
- リストは**全公開**: 誰が作ったリストも全ユーザーが閲覧・選択してガチャを引ける

## 1-2. 対象プラットフォーム

- Web版のみ。**PC・スマホ両対応のレスポンシブデザイン**とする(友人がスマホで見る可能性が高いため)

## 2. コア体験ループ

1. (布教側) リストを新規作成し、TMDB検索で映画を追加していく
2. (布教側) 作成したリストはリスト一覧に公開され、誰でも閲覧・選択可能になる
3. (迷っている側) リスト一覧から気になるリストを選ぶ
4. (迷っている側) 「ガチャを引く」ボタンを押すと、リスト内の映画から完全ランダムに1本が演出付きで選ばれる
5. 同じリストで何度でも引き直せる(重複除外などの制御は行わない、単純な等確率抽選)

## 3. MVP機能・画面

| 画面/機能 | 内容 |
|---|---|
| リスト作成・TMDB検索登録 | リスト名(+作成者表示名)を入力して新規作成。TMDB APIで映画名検索し、候補からリストへ追加(タイトル・ポスター画像・あらすじを自動取得) |
| リスト一覧・選択画面 | 全リストを一覧表示(リスト名・作成者名・収録本数など)。選ぶとガチャ画面へ |
| ガチャ演出画面 | 「ガチャを引く」操作でリスト内からランダムに1本を選出し、結果を演出付きで表示(詳細な演出仕様は別途) |
| リストの編集・削除 | 作成済みリストへの映画追加・削除、リスト自体の削除が可能 |

## 4. データモデル(概略)

- **List**: id, name, creator_name, created_at
- **ListMovie**: id, list_id, tmdb_id, title, poster_path, overview, added_at

ガチャ処理はサーバー/クライアントいずれでも良いが、`ListMovie` を `list_id` で取得し等確率で1件をランダム抽出するだけのシンプルなロジック。

## 5. 技術スタック

- **Next.js + Supabase(Postgres)** を軸に構築(サーバーレスで素早く立ち上げやすく、TMDB APIキーもNext.jsのAPI Routes経由で秘匿できる)
- TMDB APIキーはクライアントに露出させず、バックエンド(Next.js API Routes)経由で呼び出す

## 6. 今回のプロトタイプで明示的にやらないこと(スコープ外)

- 本格的な認証・アカウント管理・不正利用対策
- 多言語対応(日本語のみ)
- 視聴履歴連携(実際に見たかどうかの記録・管理)
- リスト非公開設定、リンク限定共有
- ガチャの重み付け・重複除外などの高度なロジック(完全ランダムのみ)
- 評価・コメント・いいねなどのソーシャル機能

## 7. 未決定・today要検討事項

- ガチャの演出内容の詳細(アニメーションの具体的な見た目・所要時間など)
- 表示名の重複や入力バリデーションの扱い(未検討、プロトタイプでは特に制限しない想定)
- リストの最小/最大映画数の制約有無(未検討)
